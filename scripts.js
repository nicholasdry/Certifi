// template question answers

function checkscore() {

      var score = 0;

      if (document.getElementById('q1') == PUT ANSWER HERE) {
            score++;
      }

      // make the other if-else statements for other questions

      alert(score);
}
